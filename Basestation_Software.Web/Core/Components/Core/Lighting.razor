@inject RoveCommService _RoveCommService
@inject OpService _OpState;

<div class="component-header">
    <h3>Lighting</h3>
</div>
<div class="component-body" style="display: flex; align-items: center">
    <p>R</p>
    <input style="width: 20px" @bind=r />
    <p>G</p>
    <input style="width: 20px" @bind=g />
    <p>B</p>
    <input style="width: 20px" @bind=b />
    <p>A</p>
    <input style="width: 20px" @bind=brightness />
    <button style="justify-self:center;" @onclick=Send>Send</button>
    <div style="width: 24px; height: 24px; background-color: rgb(@r, @g, @b)"></div>
</div>

@code {
    private byte r, g, b;
    private byte brightness = 0;

    private void Send()
    {
        _RoveCommService.Boards.Core.LEDRGB(r, g, b);
        _RoveCommService.Boards.Core.Brightness(brightness);
        _OpState.OpColor = Color.FromArgb(r, g, b);
    }
}